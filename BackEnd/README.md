# BoraAi BackEnd

API NestJS do projeto BoraAi.

## Rodando localmente

```bash
npm install
cp .env.example .env
npx prisma migrate dev
npm run start:dev
```

Veja o `README.md` da raiz para a visao geral do projeto.
